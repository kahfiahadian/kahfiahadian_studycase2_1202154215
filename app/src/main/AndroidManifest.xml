<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.ac.telkomuniversity.student.kahfiahadian.studycase21202154215">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logorestoran"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity
            android:name=".identitaspembeli"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".detailcustomer"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".menuMakanan" />
        <activity
            android:name=".detailMenu"
            android:parentActivityName=".menuMakanan"
            ><meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".menuMakanan"/></activity>
    </application>

</manifest>